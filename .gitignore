# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Supabase
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lock files (re-add if you want to commit them, but usually they are committed. 
# Since we just deleted them to clean Lovable registry, the user might want to re-install)
# package-lock.json
# bun.lockb
# yarn.lock
# pnpm-lock.yaml
